# 项目状态快照（保持短小）

## 当前结论（必须最新）
- 现状：`对照清单.md` 已整理成按年份分层的 Markdown 清单，阅读和后续比对都更清晰了。
- 已完成：把原先平铺的文本改成标题、分节和项目列表，并保留了原始名称、更新时间和 SHA1。
- 正在做：继续把这份清单和 `data/MANIFEST.md`、`data/raw` 的实际落地文件做对照，找出缺口和异常项。
- 下一步：如果需要，再把这份清单整理成“官网清单 vs 本地文件”的正式对照报告。

## 关键决策与理由（防止“吃书”）
- 决策A：目录统一使用 `2011-wave1` 这种命名，保留年份和波次信息。原因：既符合用户习惯，也方便后续按年份或波次检索。
- 决策B：CSV 不是唯一输出，必须同时保留 `.dta` 原件和元数据 JSON。原因：CSV 对 Stata 的标签、缺失码和部分类型信息不够完整。
- 决策C：压缩包检测要允许“能解压但有警告”的情况。原因：实际下载里存在头部异常或尾部垃圾数据，不能一刀切当成完全不可用。

## 常见坑 / 复现方法
- 坑1：2013 年的 `CHARLS2013_Dataset.zip` 当前是 0 字节，7-Zip 直接打不开。
- 坑2：2015 年的 `CHARLS2015r.zip` 会报头部错误，但内部仍能列出 `Biomarker.dta`，需要单独标注警告而不是直接放弃。
- 坑3：2011 年有一个 `TxaXB7q8.zip.part`，这不是完整压缩包，只能当未完成下载处理。
- 坑4：`Biomarker.dta` 和 `Cognition.dta` 当前读库会报 `buffer is smaller than requested size`，脚本会留下 `.error.txt`，后续需要人工复核或者换更强的读取链路。
- 坑5：根目录里的 `CHARLS2013_Dataset.zip` 不是网站公布的那一个，SHA1 和内容都对不上，不能拿来替代 `data/raw/2013-wave2/CHARLS2013_Dataset.zip`。
- 坑6：像 `对照清单.md` 这种原始平铺文本，后续最好直接按年份、文档、下载三层结构写成 Markdown，避免再手工重排。
